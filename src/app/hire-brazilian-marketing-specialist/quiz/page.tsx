import { QuizSection } from '../QuizSection'

export const metadata = {
  title: 'Apply — Merkai',
  description: 'Tell us about your business and we will get back to you shortly.',
}

export default function QuizPage() {
  return <QuizSection />
}
